# Architecture

This file is a compatibility entrypoint.

Read `llm/README.md` first.

Use:

- `llm/README.md` for the delegated instruction flow
- `llm/reference/architecture-full.md` for the full architecture reference
